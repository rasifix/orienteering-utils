import { Course } from "../utils/ranking";

export interface RunnerInfo {
    fullName: string;
    club: string;
    city: string;
    category: string;
    course: string[];
    startTime: string;
}

